[pytest]
; Nuestro código vive en src/, no en la raíz del proyecto, así que hay que
; sumarlo al path para poder hacer `from services... import ...` en los tests.
pythonpath = src
; Permite escribir tests `async def` sin decorarlos con @pytest.mark.asyncio
asyncio_mode = auto
